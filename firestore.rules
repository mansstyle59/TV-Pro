rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/favorites/{channelId} {
      allow read, write: if request.auth != null && userId == request.auth.uid;
    }
    match /config/access {
      allow read, write: if true;
    }
  }
}
